document.addEventListener('DOMContentLoaded', function () {
  const banner = document.createElement('div');

  // Text + link
  banner.textContent = '🛠️  Work in progress: this is the next, not yet published -10 iteration of ';
  const link = document.createElement('a');
  link.href = 'https://datatracker.ietf.org/doc/html/draft-davids-forsalereg';
  link.textContent = 'draft-davids-forsalereg';
  link.style.color = 'white';
  link.style.textDecoration = 'underline';

  banner.appendChild(link);

  // Styling
  Object.assign(banner.style, {
    position: 'fixed',
    bottom: '0',             // ✅ plaats aan onderkant
    left: '0',
    width: '100%',
    backgroundColor: '#ffc107',
    color: 'white',
    textAlign: 'center',
    padding: '5px',
    fontSize: '16px',
    fontWeight: 'bold',
    fontFamily: 'sans-serif',
    zIndex: '9999'
  });

  // remove explicitly on top to avoid conflicts
  banner.style.top = ''; 

  // add to <boddy> below
  document.body.appendChild(banner);

  // Make spcae for body (not needed?
  //const bannerHeight = banner.offsetHeight;
  //document.body.style.paddingBottom = bannerHeight + 'px';
});
